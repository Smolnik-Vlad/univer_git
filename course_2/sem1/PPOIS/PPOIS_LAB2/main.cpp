#include "Header.h"




int main()
{

	//	c_s->get_info(c_s);
		//a=check_str->solution("{a, b, {c, d, {g, h}}, {e}, f} + ({a, {e}, f} - ({a, {e}, f, g} * {a, b, {e}}))");
		//b = check_str->solution("{a} + {} * (( {} + {a, {a, b}} )) - {a, {b, {e, f}, c}, d}");
		//b->get_info(b);
	Read_a_line check;
	/*vector<string> str1 = {"a", "b"};
	
	vector<string> str2 = {"b", "a"};
	sort(str2.begin(), str2.end());
	cout << (str1 == str2);*/

	check.read_line();
}